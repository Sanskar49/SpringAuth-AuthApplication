## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url= jdbc:postgresql://localhost:5432/search
spring.datasource.username= postgres
spring.datasource.password= password

# The SQL dialect makes Hibernate generate better SQL for the chosen database and also springboot needs to choose.
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

#Config cred for login in spring-security

spring.security.user.name=Sanskar
spring.security.user.password=chitkara